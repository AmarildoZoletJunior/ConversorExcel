<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <table>
        <thead>
          <%  header.forEach(cabeca => { %>
                <th><%= cabeca %>  </th>
            <% }); %>
        </thead>
        <tbody>
            <%  rows.forEach(cabeca2 => { %>
                <tr> 
                    <% cabeca2.forEach(elemento => { %>
                    <td> <%= elemento %> </td>
                    <% }); %>
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>